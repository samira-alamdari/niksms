// // ============================opMenu
document.addEventListener('DOMContentLoaded', function () {
    const menuMobileIcon = document.querySelector('.mobileMenuIcon');

    menuMobileIcon.addEventListener('click', function () {
        document.body.classList.toggle('opMenu');
    });
});

// ============================opMenu